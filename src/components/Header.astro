<header class="header">
    <div class="header__wrapper">
        <a href="https://basher.biz" class="header__link">
            <img src="logo.webp" alt="basher website" height="70" width="118"/>
        </a>
    </div>
</header>

<style>
    .header {
        --color-background: 0 0% 13%;

        background: hsl(var(--color-background));
        box-shadow: 0 0.3125rem 1.875rem hsl(var(--color-background) / 0.7);
        inset: 0 0 auto;
        position: fixed;
        z-index: 1;
    }

    .header__wrapper {
        align-items: center;
        display: flex;
        margin-inline: auto;
        max-inline-size: 75rem;
        min-block-size: var(--sticky-height);
        padding: var(--gutter-s) var(--gutter-m);
    }
</style>
